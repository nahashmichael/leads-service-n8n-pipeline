# Secret checked on every request via the X-API-Key header.
# Generate with: openssl rand -hex 32
LEADS_SERVICE_API_KEY=

# Optional. Defaults to ./leads.db next to main.py.
# DATABASE_PATH=./leads.db
